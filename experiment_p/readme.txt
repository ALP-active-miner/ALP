To run Experiment P, we have to convert the relevant projects in MUBench to the graph files we expect:
To do so, run ALPPipelineCombProjectForAPIUsageGraphBuilder.java (after modifying it to point to the correct paths)

